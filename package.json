{
  "name": "tanjai-ai-studio",
  "version": "10.0.1",
  "private": true,
  "description": "ทันใจ AI Studio — Static Web App สร้างสื่อด้วย AI",
  "scripts": {
    "test": "for f in tests/*.js; do echo \"== $f ==\"; node \"$f\" || exit 1; done"
  },
  "engines": {
    "node": ">=18"
  }
}
