# V9.3.5 — Clean Image Actions

- เปลี่ยน `1. คัดลอก Prompt` เป็น `คัดลอก Prompt`
- เปลี่ยน `2. เปิด ทันใจ GPT` เป็น `เปิด ทันใจ GPT`
- คงการทำงานแบบสองปุ่มแยกกันเหมือนเดิม
- อัปเดตเลขเวอร์ชันทุกตำแหน่งเป็น V9.3.5
