const assert = require("assert");
const fs = require("fs");
const main = fs.readFileSync("index.html", "utf8");
const app = fs.readFileSync("create-video/app.js", "utf8");
const server = fs.readFileSync("server.mjs", "utf8");
const dockerignore = fs.readFileSync(".dockerignore", "utf8");
assert.match(main, /id="createVideo" class="view create-video-module"/);
assert.match(main, /data-view="createVideo"/);
assert.doesNotMatch(main, /href="create-video\//);
assert.doesNotMatch(main, /tanjai-video-studio\.onrender\.com/);
assert.match(app, /const API_BASE = location\.hostname\.endsWith\("github\.io"\)/);
assert.match(app, /version: "12\.3\.0"/);
assert.match(server, /Access-Control-Allow-Origin/);
assert.match(server, /req\.method === "OPTIONS"/);
assert.match(server, /\/api\/storyboard/);
assert.match(server, /GEMINI_API_KEY/);
assert.match(server, /geminiStoryboard/);
assert.match(server, /\/api\/produce/);
assert.match(dockerignore, /^\.env$/m);
console.log(JSON.stringify({version:"12.3.0",oneRepository:true,embeddedVideoModule:true,sharedShell:true,renderApiBridge:true,status:"PASS"},null,2));
