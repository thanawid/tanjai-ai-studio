const fs = require('fs');
const assert = require('assert');
const index = fs.readFileSync('index.html','utf8');
const js = fs.readFileSync('js/video-editor.js','utf8');
const css = fs.readFileSync('css/style.css','utf8');
const data = fs.readFileSync('js/data.js','utf8');
assert.match(index,/video-editor\.js\?v=12\.1\.0/);
assert.match(index,/อัปโหลดคลิปจำนวนมาก ปรับคุณภาพภาพ เสียง และโทนสี/);
assert.match(index,/แต่งวิดีโอ AI/);
assert.match(js,/videoFootageInput/);
assert.match(js,/processRenderQueue/);
assert.match(js,/renderClipToBlob/);
assert.match(js,/downloadSelected/);
assert.match(js,/downloadAllZip/);
assert.match(js,/พร้อมนำไปตัดต่อในโปรแกรมที่คุณถนัด/);
assert.doesNotMatch(js,/id="continueEditingBtn"/);
assert.doesNotMatch(index,/video-handoff-store\.js/);
assert.match(css,/AI Footage Assistant/);
assert.match(js,/ดาวน์โหลดคลิปที่ปรับแล้ว/);
assert.match(data,/อนิเมะร่วมสมัย/);
assert.match(data,/อนิเมะภาพยนตร์/);
assert.doesNotMatch(js,/Speech-to-Text API|Video AI API|Render Server/);
console.log(JSON.stringify({version:'12.1.0',uploadMany:true,renderQueue:true,selectedClipExport:true,zipExport:true,downloadOnly:true,status:'PASS'},null,2));
